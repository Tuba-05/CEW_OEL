{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": ["fetch.c", "-o", "fetch", "-lcurl", "-lcjson"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
